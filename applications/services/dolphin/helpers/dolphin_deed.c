#include "../dolphin.h"

// If the minimal stub interface has suppressed the stock helper files,
// bypass the compilation of internal deed weight tracking arrays completely.
#ifndef _DOLPHIN_DEED_H_

// Keep the entire file wrapped empty to preserve files without compiler errors
 
#endif // _DOLPHIN_DEED_H_